# Prospective restricted real UNI2 LoRA dynamics check

Purpose: verify that actual UNI2 adapters can optimize all classes in five epochs, NOT prove generalization superiority. Use first three training and first two validation nuclei per class in the locked UNI2 sample (50 nuclei total; separate original ROI split). Fixed balanced composition is artificial. Compare identical fresh linear-head initialization, frozen encoder versus Q/V LoRA rank8 alpha16 on blocks20-23. Cache output of frozen blocks0-19 once; this is mathematically equivalent to freezing the prefix when augmentations/dropout are disabled. No tissue, polygons as input, extra projection, auxiliary loss, or random augmentations. Cross entropy, batch5, five epochs, AdamW head .001, adapters .00001, weight decay .01, seed17. Report every epoch, positive-example adapter gradient norms, per-class recall and F1, and exact parameter counts. Do not use this tiny validation set for a superiority claim or promotion decision. Execute after main feature extraction releases RAM.

Correction before accepting the comparison: the first run allowed LoRA initialization to advance the RNG used by DataLoader, so the two arms saw different shuffles. Preserve preliminary lora_results files as invalid-for-paired-comparison. Rerun into lora_results_v2 with an independent seed17 DataLoader generator in each arm. Cache frozen final features once to avoid redundant frozen-block inference; the frozen head is mathematically unchanged. Reuse identical frozen prefix tensors and initial classifier weights. This is a control repair, not a hyperparameter change selected by validation scores.
