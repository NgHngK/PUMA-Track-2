usage: make_seed_replicates.py [-h] --base BASE --out-dir OUT_DIR
                               [--seeds SEEDS]

options:
  -h, --help         show this help message and exit
  --base BASE
  --out-dir OUT_DIR
  --seeds SEEDS
