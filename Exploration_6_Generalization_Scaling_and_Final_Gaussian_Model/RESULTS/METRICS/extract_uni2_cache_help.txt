usage: extract_uni2_cache.py [-h] --manifest MANIFEST --weights WEIGHTS --out
                             OUT [--tier-a TIER_A] [--kind {cls,tokens}]
                             [--batch-size BATCH_SIZE] [--device DEVICE]

options:
  -h, --help            show this help message and exit
  --manifest MANIFEST
  --weights WEIGHTS
  --out OUT
  --tier-a TIER_A
  --kind {cls,tokens}
  --batch-size BATCH_SIZE
  --device DEVICE
