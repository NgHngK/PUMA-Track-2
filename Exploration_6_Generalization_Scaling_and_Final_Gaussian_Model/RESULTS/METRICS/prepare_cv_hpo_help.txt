usage: prepare_cv_hpo.py [-h] --base BASE --out-dir OUT_DIR --stage
                         {optimizer,regularization,joint_refine}
                         [--trials TRIALS] [--folds FOLDS] [--seed SEED]

Prepare token-efficient grouped-CV HPO strictly inside Prompt-6 TRAIN

options:
  -h, --help            show this help message and exit
  --base BASE
  --out-dir OUT_DIR
  --stage {optimizer,regularization,joint_refine}
  --trials TRIALS
  --folds FOLDS
  --seed SEED
