usage: predict_checkpoint.py [-h] --checkpoint CHECKPOINT --manifest MANIFEST
                             --out OUT [--tier-a TIER_A]
                             [--representation REPRESENTATION]
                             [--uni2-weights UNI2_WEIGHTS] [--split SPLIT]
                             [--device DEVICE] [--num-workers NUM_WORKERS]

Inference-only Prompt-6 checkpoint prediction on a new proposal manifest

options:
  -h, --help            show this help message and exit
  --checkpoint CHECKPOINT
  --manifest MANIFEST
  --out OUT
  --tier-a TIER_A
  --representation REPRESENTATION
  --uni2-weights UNI2_WEIGHTS
  --split SPLIT
  --device DEVICE
  --num-workers NUM_WORKERS
