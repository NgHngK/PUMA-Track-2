usage: generate_hpo.py [-h] --base BASE --out-dir OUT_DIR --stage
                       {optimizer,regularization,joint_refine}
                       [--trials TRIALS] [--seed SEED]

options:
  -h, --help            show this help message and exit
  --base BASE
  --out-dir OUT_DIR
  --stage {optimizer,regularization,joint_refine}
  --trials TRIALS
  --seed SEED
