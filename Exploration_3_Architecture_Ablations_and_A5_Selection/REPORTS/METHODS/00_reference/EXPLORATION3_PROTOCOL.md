# Exploration 3 prospective protocol — before new architecture outcomes

Preserve every previous run, cache and evaluator module. Add shared Exploration3 modules without altering Exploration2 modules. Use 300 previous training features for three StratifiedGroupKFold folds (shuffle, seed17), grouped by ROI; freeze assignments in prompt3_cv.csv. Patient metadata remains unverified; historical case_id equals ROI ID. Hold the existing 150 development-validation features aside for one post-selection confirmation, explicitly not an untouched external test.

All architectures: FOV96 existing CLS cache, train-fold-only biology standardization, balanced replacement sampler N draws, ordinary CE, AdamW LR.001 WD.01, batch64, clip1, ten epochs, seeds17/29/43, matched appearance initialization and independent same-seed loader generator. Use **fixed epoch10** for CV comparison to avoid checkpoint mining; log every epoch. Aggregate held-fold predictions across the 300 rows, then exact V17 metrics. Different models across folds are expected. Original A0/A1 runs are not repeated on their old split.

Finite A-series: A0 linear; A1 current linear TierA; A2 Linear16→16 GELU Linear16→10 zero-output initialization; A3 biasless linear TierA times ten sigmoid scalar gates initialized0 (g=.5); A4 same correction times sigmoid Linear2→1 initializedzero using detached appearance entropy/log10 and top1−top2 probability margin; A5 biasless projections1536→8 and16→8, elementwise product, zero-initialized biasless8→10 residual; A6 Linear1552→32 GELU Linear32→10. A6 is a replacement bottleneck, so it cannot share an appearance-linear initialization and is explicitly identified as such.

For each A2–A6 run both a matched-capacity control using 16 fixed tanh Gaussian-projected appearance features (projection seed1701) instead of real biology and a training-row-shuffled biology control (seed+10000). These are controls of the same finite architecture family, not additional invented model types. Zero-initialized output layers include zero biases. A3/A4 are tested despite their small additional parameter counts. No rank16/HPO/LoRA expansion.

Promotion gate fixed now: candidate pooled-CV mean ROI-F1 must exceed A1 by **at least .003**, improve at least two of three seeds, exceed both its capacity and shuffled controls by at least .002 on average, have an exploratory paired ROI bootstrap95% lower endpoint >0, have no monitored class mean recall drop greater than .10 vs A1, and not enlarge the semantic train–validation F1 gap by more than .05. Stable nonsaturated gates alone are not evidence. A3 requires a substantive class-gate spread (max−min>.05); nearly equal gates supply no reason to replace A1. Rank qualifying candidates by mean ROI-F1; within .002 choose fewer parameters. If none pass retain A1. These thresholds are pragmatic predeclared development decisions, not statistical guarantees.

Confirmation: only the selected non-A1 candidate, if one qualifies, gets three fixed-epoch ten-epoch fits on the original300 train and one evaluation per seed on the original150 validation cohort. Compare against preserved A1 **final** predictions under the same epoch rule, without rerunning A1. Confirmation failure retains A1; no second-choice confirmation search. If A1 remains selected, reuse its existing results.

BioMask: five final checkpoints recovered, hash-matched to remote provenance, epoch29 zero-based. Historical folds have ROI counts47,1,86,61,10; this is not a newly generated balanced split. Every current outer split spans multiple historical BioMask folds; row-wise OOF features can depend on outer annotations through upstream training features. No checkpoint path isolates all outer groups. Therefore historical TierB is **exploratory upstream-contaminated diagnostic only and never promotion eligible**. An additional indirect Stage1-OOF-to-BioMask training path also requires nested audit. Do not train20 new detectors/mask models.

Extract one row-wise historical-fold mask for each existing450 GT point using historical reflect-padded96 crop, Gaussian sigma2.5 and validity masking. This is new GT-point inference, not relabeling historical Stage1-proposal cache. Keep sourcepoint/checkpoint hashes, UID and schema contract. Measure mask quality against the exact original GeoJSON feature exterior(s) with holes removed from raster masks; GT diagnostics never enter features. Historical20-dimensional soft-mask features are used unchanged:8 morphology,6 weighted stain/texture,1 ring contrast,3 reliability,2 ROI-relative. Binary quality metrics use threshold.5; soft geometry retains historical definitions. No mask-dependent center adjustment.

B-series diagnostics on the same three internal folds/seeds: B0 TierB linear only; B1 appearance+8 morphology; B2 appearance+6 stain/texture; B3 appearance+20 fullB; B4 appearance+16A+20B. For B3/B4 include separately training-shuffledB and fixed-appearance-projected20D B capacity controls (A remains real for B4). Reuse prior oracle as historical context only because CV splits differ; no claim of a matched new oracle result. No historical complex fusion because contaminated upstream features cannot justify promotion.

Log all exact V17 ROI/pooled P/R/F1/counts, semantic P/R/F1/accuracy/BA/NLL/ECE/prevalence/confusion, exposure, target-head gradients and model diagnostics. ROI bootstrap pairs predictions and resamples ROIs, never nuclei independently. Report all A/B results even when unfavorable. Stop after this finite family and one confirmation. Select exactly one next full-data architecture; Stage1 remains fixed.
